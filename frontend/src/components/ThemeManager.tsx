"use client";

import { useEffect } from "react";
import { API_BASE_URL } from "@/config/api";

export default function ThemeManager() {
    useEffect(() => {
        // Simple Dark Mode Sync (Optional, handled by ThemeContext)
    }, []);

    return null; // Logic only component
}
