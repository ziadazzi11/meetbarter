[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = [
  "npm install",
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "node dist/src/main.js"
